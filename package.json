{
  "name": "certownia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Darmowe certyfikaty SSL prosto w przeglądarce — klucz prywatny nigdy nie opuszcza Twojego komputera. Powered by Let's Encrypt (ACME).",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "proxy": "node proxy/node-server.mjs"
  },
  "dependencies": {
    "asn1js": "^3.0.5",
    "pkijs": "^3.2.5",
    "pvutils": "^1.1.3"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
